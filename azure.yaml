# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: sample-blazor
metadata:
  template: sample-blazor@1.0.0
services:
  web:
    project: ./src/SampleBlazor
    language: dotnet
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
      remoteBuild: true

